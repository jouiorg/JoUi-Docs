---
import './heading.css';

interface Props {
  pLevel?: 1 | 2 | 3 | 4 | 5 | 6;
  pClass?: string;
}

const { pLevel = 1, pClass, ...attrs } = Astro.props;
const Tag = `h${pLevel}` as 'h1' | 'h2' | 'h3' | 'h4' | 'h5' | 'h6';
---

<Tag class={pClass} {...attrs}>
  <slot />
</Tag>
