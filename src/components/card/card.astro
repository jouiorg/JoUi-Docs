---
import './card.css';

interface Props {
  pClass?: string;
}

const { pClass, ...attrs } = Astro.props;
const classes = ['card', pClass].filter(Boolean).join(' ');
---

<article class={classes} {...attrs}>
  <slot />
</article>
